{
  "name": "iqbalperkasa.com",
  "scripts": {
    "dev": "parcel index.pug",
    "build": "rm -rf build/ && parcel build index.pug -d build"
  },
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "pug": "^2.0.4",
    "sass": "^1.22.10"
  }
}
